name: Keep Streamlit Alive

on:
  # 手动触发
  workflow_dispatch:
  # 定时触发：每 15 分钟一次
  schedule:
    - cron: "*/15 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: curl -s https://magnitorammed.streamlit.app/ > /dev/null


